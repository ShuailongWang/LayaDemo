/**
 * PendulumScenne.js
*/
export default class PendulumScenne extends Laya.Scene {
    constructor() {
        super();
    }

    onEnable() {
        // var hook = this.getChildByName('hook');
        // var ball = this.getChildByName('ball');

        // Laya.timer.frameLoop(1, this, function() {
        //     this.graphics.clear();
        //     this.graphics.drawLine(hook.x + 15, hook.y + 15, ball.x + 50, ball.y + 50, "#0000ff", 10);
        // });
    }
}